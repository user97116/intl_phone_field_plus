name: intl_phone_field_plus
description: A customized Flutter TextFormField to input international phone number along with country code.
version: 2.1.1
repository: https://github.com/vanshg395/intl_phone_field
publish_to: none

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/flags/
